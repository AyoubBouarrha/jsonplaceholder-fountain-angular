# jsonplaceholder-fountain-angular


## Install dependencies 

Run `npm install` for npm dependencies

## Development

Run `npm run serve` for lunching the server.

Open `http://localhost:3000` for the preview

